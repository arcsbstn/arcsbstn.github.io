import styled from "styled-components";

export const Chip = styled.span`
  font-size: 0.8em;
  padding: 0.5em 0.8em;
  margin-right: 0.8em;
  background-color: rgba(0, 0, 0, 0.1);
  border-radius: 1.2em;
`;
